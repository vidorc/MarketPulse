# Base URL of the MarketPulse FastAPI backend.
# Browser-side fetches use this, so it must be reachable from the user's browser
# (i.e. localhost:8000), not the in-Docker hostname.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
